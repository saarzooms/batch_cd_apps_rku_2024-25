import 'package:flutter/material.dart';

void main() {
  runApp(
    Container(
      color: Colors.green,
      padding: const EdgeInsets.all(16),
      child: Container(
        color: Colors.yellow,
      ),
    ),
  );
}
